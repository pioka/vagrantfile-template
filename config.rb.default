base_index = File.basename(__dir__).match(/^\d\d/)[0].to_i

Hosts = []

Hosts.push({
  hostname: "examplehost",
  cpus: 1,
  memory_mb: 512,
  #external_disk_size_mb: 1024,
  private_ip: "192.168.222.#{base_index+Hosts.length}",
  ssh_forward_port: 22200+base_index+Hosts.length,
})
